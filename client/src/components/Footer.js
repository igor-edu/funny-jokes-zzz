import React from 'react';
import { Alert } from 'react-bootstrap';

const Footer = () => {
  return (
    <Alert className='Footer' variant='secondary'>
      Made with love @jokes.fanatic
    </Alert>
  );
};

export default Footer;
